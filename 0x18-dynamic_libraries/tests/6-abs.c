#include "main.h"
/**
 * _abs - program returns the absolute value of a number
 * @n: number to be checked
 *
 * Return: Absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
