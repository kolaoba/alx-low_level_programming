#include "pymain.h"
/**
 *  add- program returns the addition of two integers
 * @a: first number to be added
 * @b: second number to be added
 *
 * Return: Addition of a and b
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
