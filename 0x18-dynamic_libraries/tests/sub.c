#include "pymain.h"
/**
 * sub - program returns the subtraction of two integers
 * @a: first number
 * @b: second number
 *
 * Return: Subtraction of a and b
 */
int sub(int a, int b)
{
	int diff;

	diff = a - b;
	return (diff);
}
